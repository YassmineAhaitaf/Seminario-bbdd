---
title: "Seminario Bases de Datos"
author: "Aya Toussi , Yassmine Ahaitaf"
date: "`r Sys.Date()`"
output: html_document
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = TRUE)
```

## Creando una conexión a una base de datos local usando R

Primero , para poder ejecutar los comandos de SQL en un documento de RMarkdown , deberemos utilizar los paquetes DBI y RPostgres

Incluimos el código que viene a continuación que nos ha permitido conseguir generar una conexión local. 

```{r echo=TRUE}
#Cargar el paquete RPostgres

install.packages("DBI")
install.packages("RPostgres")

library(DBI)
library(RPostgres)


#Creando una conexión a la base de datos PostgreSQL 

con <- dbConnect(RPostgres::Postgres(),
                 dbname = "postgres",  
                 host = "localhost",          
                 port = 52719,               
                 user = "postgres",          
                 password = "postgres")
```

## Including Plots

You can also embed plots, for example:

```{r pressure, echo=FALSE}
plot(pressure)
```

Note that the `echo = FALSE` parameter was added to the code chunk to prevent printing of the R code that generated the plot.
